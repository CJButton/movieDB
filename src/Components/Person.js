import React from 'react'
// import PropTypes from 'prop-types'

/**
 * Needs to display --------
 */
const Person = (props) => {
  return(
    <div>
      Person
    </div>
  )
}

export default Person